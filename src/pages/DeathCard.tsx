/**
 * PLACEHOLDER — built in Steps 27-28 (§22.6).
 * Owned by exactly one agent; do not edit from another step.
 */
export function DeathCard() {
  return <div className="page-placeholder">the death card</div>;
}
