/**
 * PLACEHOLDER — built in Step 23 (§22.5).
 * Owned by exactly one agent; do not edit from another step.
 */
export function MoneyPage() {
  return <div className="page-placeholder">/money — portfolio and allocation</div>;
}
