/**
 * PLACEHOLDER — built in Step 17 (§22.1).
 * Owned by exactly one agent; do not edit from another step.
 */
export function Home() {
  return <div className="page-placeholder">/home — the dashboard</div>;
}
