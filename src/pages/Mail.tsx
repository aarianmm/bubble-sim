/**
 * PLACEHOLDER — built in Step 18 (§22.2).
 * Owned by exactly one agent; do not edit from another step.
 */
export function Mail() {
  return <div className="page-placeholder">/mail — the inbox</div>;
}
