/**
 * Application root. Owns nothing but composition (§25.1: /ui is glue).
 * The chrome shell replaces this placeholder in Step 12.
 */
export function App() {
  return <div className="app-root">BUBBLE — scaffold</div>;
}
